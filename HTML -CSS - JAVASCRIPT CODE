<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>Smart Weather Website</title>
  <link href="https://fonts.googleapis.com/css2?family=Poppins:wght@400;600&display=swap" rel="stylesheet">
  <style>
    body {
      font-family: 'Poppins', sans-serif;
      background: linear-gradient(to right, #4facfe, #00f2fe);
      display: flex;
      flex-direction: column;
      align-items: center;
      justify-content: center;
      min-height: 100vh;
      margin: 0;
      color: #333;
    }
    h1 {
      margin-bottom: 20px;
      font-size: 2.5rem;
      color: white;
      text-shadow: 1px 1px 4px rgba(0,0,0,0.2);
    }
    .search-box {
      display: flex;
      gap: 10px;
      margin-bottom: 30px;
    }
    input {
      padding: 12px 16px;
      border-radius: 12px;
      border: none;
      width: 250px;
      font-size: 1rem;
      outline: none;
    }
    button {
      padding: 12px 20px;
      border-radius: 12px;
      background: #003cff;
      color: white;
      border: none;
      font-size: 1rem;
      cursor: pointer;
      transition: background 0.3s ease;
    }
    button:hover {
      background: #001fbf;
    }
    .card {
      background: white;
      padding: 25px 30px;
      border-radius: 20px;
      box-shadow: 0 6px 20px rgba(0,0,0,0.1);
      text-align: center;
      width: 300px;
      transition: transform 0.3s ease;
    }
    .card:hover {
      transform: translateY(-5px);
    }
    .icon {
      font-size: 60px;
      margin: 15px 0;
    }
    .temp {
      font-size: 2rem;
      font-weight: bold;
      margin-bottom: 5px;
    }
    .desc {
      font-size: 1.1rem;
      color: #555;
    }
  </style>
</head>
<body>
  <h1>🌤 Smart Weather App (Demo)</h1>
  <div class="search-box">
    <input type="text" id="cityInput" placeholder="Enter city name (e.g. Delhi, Mumbai)" />
    <button onclick="getWeather()">Search</button>
  </div>

  <div id="weatherCard" class="card" style="display:none">
    <h2 id="cityName"></h2>
    <div id="icon" class="icon">☀</div>
    <p id="temp" class="temp"></p>
    <p id="description" class="desc"></p>
  </div>

  <script>
    const dummyWeather = {
      Delhi: { temp: 34, desc: 'Sunny', icon: '☀' },
      Mumbai: { temp: 30, desc: 'Humid and Rainy', icon: '🌧' },
      London: { temp: 18, desc: 'Cloudy', icon: '☁' },
      Paris: { temp: 22, desc: 'Clear Sky', icon: '☀' },
      Tokyo: { temp: 25, desc: 'Light Rain', icon: '🌦' },
      NewYork: { temp: 28, desc: 'Partly Cloudy', icon: '⛅' },
      Bangalore: { temp: 26, desc: 'Moderate Rain', icon: '🌧' },
      Jaipur: { temp: 35, desc: 'Hot and Dry', icon: '🔥' }
    };

    function getWeather() {
      const city = document.getElementById('cityInput').value.trim();
      const data = dummyWeather[city];

      if (!data) {
        alert('City not found in demo data!\nTry: Delhi, Mumbai, London, etc.');
        return;
      }

      document.getElementById('weatherCard').style.display = 'block';
      document.getElementById('cityName').innerText = city;
      document.getElementById('temp').innerText = `${data.temp}°C`;
      document.getElementById('description').innerText = data.desc;
      document.getElementById('icon').innerText = data.icon;
    }
  </script>
</body>
</html>
